# ATS Current Progress

Completed:
- Set up front-end framework
- Successfully ran `npm install` and `npm build`
- UI for showing jobs
- UI for adding jobs
- Design the SQLite schema
- Configured API endpoints in `api.php`
- Confirmed API works through CURL and in Browser (jobs are showing on web page)

Current Tasks:
**CORS Configuration:** Ensure the backend allows requests from the frontend (if running on different ports during dev).

Current Issues:
none