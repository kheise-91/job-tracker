services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ats
    restart: unless-stopped
    ports:
      - 9000:80
    volumes:
      - ./data:/var/www/html/data
    environment:
      - APP_ENV=production

volumes:
  job-tracker-db: