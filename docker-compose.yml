services:
  ats:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ats
    restart: unless-stopped
    ports:
      - 9000:80
    volumes:
      - ./data:/var/www/html/data:rw
    environment:
      - APP_ENV=production